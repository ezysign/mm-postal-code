{
  "name": "mm-nrc-verify",
  "version": "1.0.0",
  "description": "Can log \"hello world\" and \"goodbye world\" to the console!",
  "main": "src/main.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "ts-node-dev src/main.ts",
    "convert": "ts-node-dev convert2json.ts",
    "test": "jest --verbose --coverage --watchAll",
    "build:dist": "rm -rf dist && tsc"
  },
  "devDependencies": {
    "csv-parser": "^3.0.0",
    "jest": "^27.3.1",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "ts-jest": "^27.0.7",
    "ts-node-dev": "^1.1.8"
  }
}
